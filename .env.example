# Required for write operations (send USDC, create jobs, give reputation, etc.)
# Use a testnet-only wallet. Never commit a real private key.
PRIVATE_KEY=0xREDACTED_LEAKED_TESTNET_KEY

# Optional: override the Arc Testnet RPC endpoint
# ARC_RPC_URL=https://rpc.testnet.arc.network
