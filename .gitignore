# Dependencies
node_modules/
**/node_modules/

# Build outputs (dist/ is committed so users can run without building)
.next/
out/

# Env files — NEVER commit private keys or API keys
.env
*.env
.env.local
.env.*.local

# Foundry
cache/
**/cache/
broadcast/
**/broadcast/
# forge-std and other deps — installed via `forge install` (see README)
multi-evaluator/lib/

# Misc
.DS_Store
*.js.map
tsconfig.tsbuildinfo
*.tsbuildinfo

# Repo-specific
extracted/
.claude/
*.zip

.vercel
